# Plan 001: CHANGELOG reflects v3, and README claims match reality

> **Executor instructions**: Follow this plan step by step. Run every
> verification command and confirm the expected result before moving to the
> next step. If anything in the "STOP conditions" section occurs, stop and
> report — do not improvise. When done, update the status row for this plan
> in `plans/README.md`.
>
> **Drift check (run first)**: `git diff --stat 591af05..HEAD -- CHANGELOG.md README.md cliff.toml`
> If any in-scope file changed since this plan was written, compare the
> "Current state" excerpts against the live code before proceeding; on a
> mismatch, treat it as a STOP condition.

## Status

- **Priority**: P1
- **Effort**: S
- **Risk**: LOW
- **Depends on**: none
- **Category**: docs
- **Planned at**: commit `591af05`, 2026-06-15

## Why this matters

`CHANGELOG.md`'s last entry is **v1.1.3** but the package is at **v3.0.0** — two
major versions of changes (monorepo move, v2 virtual-module rewrite, v3 Vite 7/8
migration) are undocumented for users and contributors. Separately, the README
makes a few claims the repo doesn't back up, which erodes trust in the docs.
`git-cliff` is already configured (`cliff.toml`) and follows Conventional
Commits — regenerating the changelog is mechanical and keeps it from drifting
again. (Conventional Commits + Keep a Changelog + git-cliff remains the current
2025–2026 standard: https://git-cliff.org/, https://www.keepachangelog.com/.)

## Current state

- `CHANGELOG.md` — last section is `## [1.1.3] - 2025-04-04`; generated by
  git-cliff (`<!-- generated by git-cliff -->` footer).
- `cliff.toml` — git-cliff config, present at repo root (already tuned for this
  repo; do not rewrite it).
- `package.json:version` = `3.0.0`; `packages/plugin/package.json:version` =
  `3.0.0`.
- **No `v3.0.0` git tag exists.** Remote tags stop at the `v1.2.x` series
  (verify with `git tag`). git-cliff normally derives version sections from
  tags, so the v2/v3 commits currently live in git-cliff's "unreleased" bucket.
- README claims to fix:
  - `README.md:226` lists **Angular** in the "Framework Agnostic" logo strip and
    `README.md:235` says "React, Vue, Svelte, Angular, Solid, Lit, and more."
    The repo's `test-apps/` only contains: `react-test-app`, `vue-test-app`,
    `sveltekit-app`, `nuxt-app`, `tanstack-start`. There is no Angular/Solid/Lit
    coverage. (Angular does not use Vite as its default build pipeline.)
  - `README.md:281-298` "Development > Testing" mentions only `npm test` /
    `test:watch` / `test:coverage` and says "Tests are in
    `packages/plugin/tests/`". It omits the **extended** suite
    (`packages/plugin/tests-extended/`, run via `test:unit-extended`) and the
    **cross-framework harness** (`tests/harness.test.ts`, run via
    `test:harness`). Both exist and run in CI (`.github/workflows/test.yml`).

## Commands you will need

| Purpose | Command | Expected on success |
|---|---|---|
| Install | `bun install --frozen-lockfile` | exit 0 |
| Generate changelog | `bun x git-cliff --tag v3.0.0 -o CHANGELOG.md` | exit 0, file rewritten |
| Inspect git tags | `git tag` | list ending at the v1.2.x series |
| Verify markdown | visual diff of `CHANGELOG.md` | top section is `[3.0.0]` |

> If `bun x` is unavailable, `npx git-cliff ...` is equivalent. `git-cliff` is
> already a devDependency.

## Scope

**In scope** (the only files you should modify):
- `CHANGELOG.md` (regenerate)
- `README.md` (targeted text edits only)

**Out of scope** (do NOT touch):
- `cliff.toml` — the changelog config is already correct; rewriting it changes
  every entry's format.
- `package.json` versions — releasing/bumping is a separate concern.
- Any source file under `packages/plugin/src/`.

## Git workflow

- Branch: `advisor/001-changelog-readme` (or the repo convention if evident).
- Commit message style: Conventional Commits (the repo uses them — see
  `git log --oneline`). Example: `docs: regenerate changelog for v3 and fix README claims`.
- Do NOT push or open a PR unless the operator instructed it.

## Steps

### Step 1: Regenerate the changelog through v3.0.0

Run `bun x git-cliff --tag v3.0.0 -o CHANGELOG.md`. The `--tag v3.0.0` flag
folds all currently-unreleased commits into a new `[3.0.0]` section (no tag is
required to exist). Open `CHANGELOG.md` and confirm the top-most version section
is now `## [3.0.0]` and that the older `[1.1.3]`…`[initial]` sections are still
present below it.

**Verify**: `head -5 CHANGELOG.md` → shows the `# Changelog` heading; searching
the file for `[3.0.0]` returns a match near the top:
`grep -n "\[3.0.0\]" CHANGELOG.md` → one match with a low line number.

### Step 2: Fix the README framework claims

In `README.md`, make the "Framework Agnostic" section truthful. There are THREE
untested-framework references to remove (all confirmed present):
- the **Angular** logo `<img>` (around `README.md:226`)
- the **Solid** logo `<img>` (around `README.md:228`)
- the **Lit** logo `<img>` (around `README.md:230`)
- the sentence at `README.md:235`: "React, Vue, Svelte, Angular, Solid, Lit, and
  more."

Prefer: soften the sentence to "Works with any Vite-based framework" and keep
only logos for frameworks that have a `test-apps/` entry (React, Vue, Svelte;
SvelteKit/Nuxt/TanStack Start are meta-frameworks of those). Do not invent new
claims. (Removing all three untested logos is also acceptable.)

**Verify** (all three must return nothing — these grep the alt/title text inside
the `<img>` tags too, so a leftover logo is caught):
- `grep -in "angular" README.md` → no output
- `grep -in "solid" README.md` → no output
- `grep -in "\blit\b" README.md` → no output

### Step 3: Fix the README "Development > Testing" section

Update the Testing section to document all three suites. (`## Development` is at
`README.md:281`, `### Testing` at ~`283`, and the inaccurate "Tests are in
`packages/plugin/tests/`" line at ~`298`.)
- `npm test` / `bun run test` — core unit tests (`packages/plugin/tests/`).
- `npm run test:unit-extended` — extended unit suite
  (`packages/plugin/tests-extended/`).
- `bun run test:harness` (from repo root) — cross-framework integration harness
  (`tests/harness.test.ts`, builds the `test-apps/`).

Keep it concise and match the existing bullet style.

**Verify**: `grep -n "test:harness\|test:unit-extended" README.md` → at least one
match each.

## Test plan

No code changes, so no new automated tests. Verification is the `grep` checks
above plus a manual read of the regenerated `CHANGELOG.md` top section.

## Done criteria

ALL must hold:

- [ ] `grep -n "\[3.0.0\]" CHANGELOG.md` returns a match in the first ~40 lines
- [ ] The `[1.1.3]` and `[initial]` sections still exist in `CHANGELOG.md`
- [ ] `grep -n "test:harness" README.md` returns a match
- [ ] `grep -n "test:unit-extended" README.md` returns a match
- [ ] `grep -in "angular" README.md`, `grep -in "solid" README.md`, and
      `grep -in "\blit\b" README.md` each return nothing (untested-framework
      references removed)
- [ ] `git status` shows only `CHANGELOG.md` and `README.md` modified
- [ ] `plans/README.md` status row updated

## STOP conditions

Stop and report back (do not improvise) if:

- `git-cliff` produces an empty or obviously wrong changelog (e.g. only a v3.0.0
  section with the older history deleted) — the `cliff.toml` may expect a
  different invocation; report the actual output instead of hand-editing.
- A `v3.0.0` (or higher) git tag already exists — the correct invocation may be
  plain `bun x git-cliff -o CHANGELOG.md`; report and ask.
- The README's framework list turns out to be backed by `test-apps/` you didn't
  see (re-list `test-apps/` first).

## Maintenance notes

- Make `git-cliff` part of the release step so the changelog never drifts again:
  the repo's `release` script (`bumpp`) could be paired with
  `git-cliff --bump -o CHANGELOG.md` (or `--tag <new-version>`) on tag. Note this
  for the maintainer; implementing it is out of scope here.
- A reviewer should confirm the regenerated changelog's grouping (Features / Bug
  Fixes / Misc) matches the prior style produced by `cliff.toml`.
